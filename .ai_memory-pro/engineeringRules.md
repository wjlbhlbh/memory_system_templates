# 工程治理细则 (Engineering Rules)

## 1. 证据与结论
- 所有结论必须基于至少一种真实证据：代码、测试、日志、报错、文档或运行结果。
- 没有证据时，只能给出“假设”或“待确认项”，不能给出确定性结论。
- 不伪造测试结果、日志、命令输出、截图或运行结论。

## 2. 风险分级
- L0：文档修改、样式微调、文案调整、不影响行为的重构、小型辅助函数。可直接执行。
- L1：一般功能开发、普通 bug 修复、非核心模块重构、新增单元测试、小范围接口改动。默认直接执行。
- L2：数据库结构变更、批量数据修改、认证/授权/支付/计费逻辑改动、公共 API 行为变化、构建部署配置变更、重要新依赖、跨模块大改。必须先确认。
- L3：删除重要数据、不可逆迁移、生产环境操作、安全策略变更、可能导致服务中断的基础设施调整。默认不自动执行。

## 3. 需求澄清与意图翻译
- 实施前必须明确：用户原话、真实意图、成功标准、明确非目标、任务分级。
- 用户不懂技术细节是常态，不得因为表达不专业就按字面扩写需求或直接动代码。
- 若存在多个合理解释，默认选择最小、可回退、最不破坏既有行为的一种。
- 只要歧义会影响数据、兼容性、公共行为、权限边界、依赖、部署或架构，就必须先停在方案阶段确认。
- 复杂任务开始前必须写出 Requirement Checklist 草案，把用户要求、边界、非目标、验收命令逐条列出。
- 完成前必须复核 Requirement Checklist，任何未实现或未验证项都必须如实标为未覆盖，不得用“整体完成”掩盖。

## 4. 必须先确认的操作
- 修改数据库 schema、执行迁移、批量导入/更新/删除数据。
- 修改认证、授权、风控、支付、计费相关逻辑。
- 修改公共 API 的输入、输出、状态码、字段语义。
- 删除或重命名关键文件、关键表、核心类、公共函数。
- 引入会影响构建、体积、许可证或运行方式的新依赖。
- 修改 CI/CD、Docker、Nginx、环境变量、密钥加载方式。
- 触发部署、发布、回滚、生产环境命令。
- 大规模重构或任何不可逆操作。

## 5. 编辑与兼容
- Search/Replace 前必须确认上下文仍匹配当前物理文件。
- 禁止为了方便而整文件重写。
- 禁止擅自删除历史记忆、历史日志或用户已有改动。
- 指令优先级：用户明确要求 > 真实环境约束 > 仓库现有约定 > 框架惯例 > 本模板规则 > 个人偏好。
- 修改公共接口时必须识别调用方，优先兼容；若不能兼容，必须明确标记 breaking change。

## 6. 范围保护与回归验证
- 修改前先列出“禁止误伤项”：不应被破坏的模块、页面、接口、数据流、测试口径、部署路径。
- 未验证前不得宣称“只影响这里”或“其他模块不会受影响”。
- 修复 bug 或实现功能时，至少对直接相邻能力做一次回归验证，避免修一处坏一片。
- 遇到跨模块改动时，优先拆成多个可验证阶段，而不是一次性大改。
- 如果任务不能在一次会话内独立验证，必须拆到 `masterTaskLedger.md`；每个子任务应能独立完成、独立验证、独立交接。
- 如果上下文超过单次可控范围，必须创建 `task-packs/*.md`，明确 required reading、do not read、acceptance、handoff。

## 7. 安全底线
- 所有外部输入默认不可信，必须验证、约束、清洗或转义。
- 禁止硬编码密码、密钥、令牌、连接串。
- 禁止在日志和报错中泄露敏感信息。
- 涉及鉴权、权限边界、支付、导出、Webhook、文件上传、管理后台时，默认按高风险处理。

## 8. 源码提交与本地备份分层
- 任务完成并验证后，默认执行：`git status` -> `git add` -> `git commit -m "中文提交名"` -> `git push`。
- 源码、文档、测试、配置的正常修改进入 Git。
- 本地备份统一放入 `.archive/`，并确保 `.archive/` 写入 `.gitignore`。
- 本地数据库、日志、缓存、临时目录、运行产物必须写入 `.gitignore`。
- 若运行产物已经被 Git 跟踪，使用 `git rm --cached` 取消跟踪并保留本地文件。

## 9. 连续开发、并行协作与恢复协议
- `activeContext.md` 是指针型启动胶囊；进入多轮任务后，只在状态、任务 ID、verified checkpoint、下一步或恢复指针变化时更新，详细过程进入任务包。
- `progress.md` 只记录已验证事实；长任务按 checkpoint 持续写入，避免上下文压缩后丢失阶段成果。
- `activeContext.md` 与 `progress.md` 属于单写者文件；多 Agent 并行时只允许主 Agent (main agent) 写入，子 Agent 只能回传：目标边界、修改文件、验证结果、未解决风险。
- 多 Agent、长任务、跨模块任务必须使用 `masterTaskLedger.md` 记录状态、locked files、verification evidence 和交接要点。
- 单任务上下文过大时使用 `task-packs/`，且恢复时只读相关任务包，不重读全量历史。
- 长期历史、过期交接和大段日志进入 `history/`，不得让 `activeContext.md`、`progress.md`、`masterTaskLedger.md` 承担归档库职责。
- 禁止子 Agent 直接改主记忆，避免并发写入、状态冲突和覆盖主线恢复锚点。
- 发生 context compression、模型切换、工具切换或人工接管时，只重读 `activeContext.md`；确认续接同一任务后，再按其中精确列出的任务包和 Resume Reads 恢复。
- 恢复时禁止依赖“聊天里大概提过什么”的印象式记忆，必须回到文件化状态。

## 10. 完成门禁 (Definition of Done)
- `[DONE]` 只能用于 Requirement Checklist 全部已实现或已明确非目标，且关键项都有 verification evidence 的任务。
- 缺少真实执行证据时，状态只能是 `[PARTIAL]`、`[AWAITING_QA]`、`[BLOCKED]` 或 `[REWORK]`。
- 用户要求的细节、边界、异常路径、权限边界、兼容性要求必须逐条回应；不能只交付“核心 Demo”。
- 如果某项无法自动化验证，必须写明人工验证步骤、未执行原因和剩余风险。

## 11. 文件编码与完整性
- Agent 可读文件默认使用 UTF-8 无 BOM，尤其是 `.md`、`.json`、`.yaml`、`.yml`、`.txt`、工具入口模板。
- 写文件必须显式指定编码，禁止依赖任何工具、编辑器或系统默认编码。
- 修改记忆文件时，任何工具、命令、编辑器或补丁方式都可以使用，但必须显式保持 UTF-8 无 BOM，不得依赖默认编码或隐式文本输出 (default encoding / implicit text output)。
- 读取失败、乱码、BOM 导致解析异常、JSON 无法解析时，先修复可读性再继续业务改动。
- 修改记忆文件后应检查：关键文件可读、`index.json` 可解析、`startup_order` 仅含 `activeContext.md` 且预算未超限。

## 12. 前端 UI 设计与防堆砌 (UI Design & Anti-Pile-Up)
- 适用范围：任何项目的所有前端界面 (Web / 移动端 / 管理后台 / 用户前台 / 小程序等)。
- 核心立场：治本靠信息架构与分层，不靠审美；用规则代替品味，用一致性兜底质量。堆砌的本质不是“东西多”，而是没做信息分层——东西多≠堆砌，平铺才是。
- 一句话总则：先想“这块屏幕给谁用、让他干一件什么事”，再决定放什么；能用“收进二级导航/标签页/抽屉”解决的，绝不平铺到一屏。商务级 = 结构清晰、首屏即重点、长内容有去处。

### 12.1 质量标尺（触发改造的判定线）
| 现象 | 判定 | 处置 |
|---|---|---|
| 单屏需滚动超过约 1.5 个视口才看完非必要内容 | 长页面 | 拆模块 / 上提概览 / 列表封顶 |
| 同屏出现超过约 4–5 个平级功能块 | 堆砌 | 二级导航互斥切换 |
| 长列表每行都把完整详情铺开 | 不合格 | 行→抽屉看详情 |
| 概览 / KPI 被埋在页面下方 | 不合格 | 上提首屏 |
| 只画了有数据一种状态 | 没画完 | 补齐四态 |
| 窄屏横向溢出 / 导航变形 | 破版 | 响应式 + 窄屏规则 |

### 12.2 信息分层（先分层，后视觉）
- 一个屏幕 = 一个主要任务；出现第 5–6 个平级区块时，应进标签页或抽屉，而非继续平铺。
- 新功能先问“和现有哪个模块是同类”：多数新增是已有功能的变种，应做成现有界面里的字段/选项/子步骤，而非新开整块；仅当使用者或目标完全不同才新开顶层入口。
- 优先级收口：必看信息置顶 → 常用功能常驻 → 偶用功能收进二级/抽屉 → 可省信息直接删。
- 顶层导航保持精简（≤ 5–7 项），超出则分组或折叠，避免一屏塞满入口。

### 12.3 长页面改造手法（可组合）
- **A. 二级导航互斥切换**：平铺多块收为二级导航，任意时刻只展示一个模块（用互斥可见状态，不靠滚动定位）。
- **B. 概览上提 + 抽屉下钻**：顶部放概览条；长列表行内只留主值，点击行在右侧抽屉展示完整详情；偶用区块收抽屉 / 折叠。
- **C. 列表封顶**：列表统一分页或懒加载；单块长列表用“内部滚动封顶”（容器高度用令牌约束，不撑高整页）。
- 组合选择：多块平铺→A；行信息密 / 概览被埋→B；单列表过长→C。

### 12.4 新增 UI 前自检 5 问（答不出就先别做）
1. 这屏给谁用？（角色/用户画像）
2. 他来这屏最想完成的 Top 3 任务？（其余皆次要）
3. 哪些信息必须一眼看到？→ 置顶
4. 哪些偶尔才用？→ 收进二级导航/标签页/抽屉
5. 哪些其实可以不显示？→ 直接删

### 12.5 文字克制（禁冗余描述、禁开发语言混入 UI）
- 标签尽量短（几个字）；仅当字段不直观才给一行辅助说明。
- 删掉“说明/简介”类段落（多为把标题重述一遍，无增量信息）。
- 按钮写动作词（如“导出”），不写长句（如“点击此处进行导出操作”）。
- 数字 > 长句：直接呈现指标，不用一句话去描述指标。
- 空态/加载中/出错用占位或轻提示（Toast/Skeleton）表达，不堆整段说明文字。
- 严禁把变量名、接口字段、报错码、TODO、调试日志当作界面文字显示。

### 12.6 设计令牌系统（品牌与一致性）
- 所有视觉取值集中在令牌层，组件只引用令牌，禁止内联字面量。
- 令牌分类：颜色（按语义：主色 / 背景 / 表面 / 正文 / 次要 / 弱化 / 描边 / 成功 / 警告 / 危险 / 信息，各分档）、字体（族 / 字号阶梯 / 字重 / 行高）、间距（基于小单位栅格）、圆角、阴影（轻量）、动效（快 / 中 / 慢）。
- 语义优先：组件引用语义令牌而非具体色；同一语义状态（待审核/成功/失败）在全站用同一种颜色 + 同一种文案，禁止多套并存。
- 暗色模式：同语义、反相映射，不新增语义；令牌预留暗色能力。
- 沿用项目既有品牌色令牌，不擅自引入新色板或改换色调；改品牌色前必须先确认。
- **具体色值 / 尺寸以目标项目品牌系统为准，本规范不给定值。**

### 12.7 通用组件词汇
- 建立一套跨页面共用的 UI 原语并固定命名（可按团队习惯调整，但全站统一）：二级导航 / 工作区容器 / 右侧抽屉 + 遮罩 / 概览条 / 面板卡片 / 四态占位 / 详情网格（标签—值）/ 列表堆叠容器 / 领域卡片基类（可派生紧凑变体）。
- 约定：抽屉必配遮罩；导航激活态单一类；可交互元素 focus 可见。

### 12.8 四态规范
- 必须同时设计四种状态：有数据、空态、加载中、出错；只画“有数据”等于没画完。
- 空态说明“为何为空”+ 一句引导；加载中用骨架屏且与真实内容同形；出错用用户语言 + 重试入口，不抛接口原文。

### 12.9 响应式与窄屏
- 定义 3–4 档断点（桌面 / 平板 / 手机），每档不破版；断点用令牌表达。
- 窄屏规则：左导航收为顶部横向标签条；多列堆叠为单列；表格允许横向滚动不挤压；抽屉满宽。

### 12.10 可访问性（a11y，交付常查）
- 颜色对比度满足 WCAG AA（正文 4.5:1，大字 3:1）。
- 键盘可完整操作，focus 状态清晰可见。
- 语义化标签 + 必要 ARIA；触摸目标 ≥ 44px；尊重 reduced-motion。

### 12.11 命名一致
- 导航名 = 页面标题 = 按钮文案，三处必须对得上，避免用户迷路。

### 12.12 多角色与权限
- 同屏不同权限看到的内容不同，设计时需覆盖；功能在权限不足/下架时要能隐藏，不能只想着“上”不想“收”。

### 12.13 重构边界纪律
- 纯 UI 重构只加纯 UI 状态变量；不动业务逻辑 / 接口 / 权限 / 路由 / 文案；不引入新依赖；复用既有令牌与组件，不新建样式方言。

### 12.14 验收清单
- 首屏核心信息一眼可见，无过长无关滚动。
- 含多块的页默认只展示一个模块。
- 长列表分页 / 懒加载；行详情走抽屉；列表高度收敛。
- 四态齐全；各断点不破版（窄屏导航收顶部标签条）。
- 同语义同色同话；颜色用令牌未擅自换色。
- 对比度达标；键盘可操作。
- 导航名 = 标题 = 按钮文案；文字克制无废话。
- （纯重构）未动逻辑 / 接口 / 权限 / 路由 / 文案；未引新依赖。
- 构建 + 回归全绿；有截图证据。

### 12.15 交付流程与执行纪律
- 先出静态稿/原型给人看，再写代码，早期即可揪出堆砌。
- 流程：原型确认 → 落地（令牌 + 通用组件）→ 构建 / 类型检查绿 → 回归 + 截图 → 提交推送备份。
- 交付时不破坏任何现有功能；质量按交付标准；状态/文案全站统一。

### 12.16 自测口诀
> 这屏一件事，主次分得清；常用的留，偶用的收，没用的删；状态同色同话，文字短到能扫。
