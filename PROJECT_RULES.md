# AI 编程项目开发规则

> 目标：让 AI 编程工具在复杂项目中也能稳定地产出可验证、可维护、可回滚的结果。  
> 适用对象：AI 编程助手、非专业开发者、协作式项目开发。  
> 适用范围：Web、后端、前端、脚本、自动化、数据处理、中小型到较复杂的软件项目。  
> 文档定位：这是“执行规则”，不是“建议清单”。AI 在项目中应默认遵守。

---

## 1. 总原则

### 1.1 先理解，再修改
- 不允许在未理解需求、现有代码和影响范围前直接写代码。
- 不允许靠猜测修复 bug。
- 不允许把“看起来可行”当成“已经正确”。

### 1.2 先证据，再结论
- 所有结论必须基于至少一种证据：代码、测试、日志、报错、文档、运行结果。
- 没有证据时，只能给出“假设”，不能给出“确认结论”。

### 1.3 小步修改，持续验证
- 优先做小范围、可验证、可回退的改动。
- 每次改动后都要做最小必要验证，避免累积到最后一起爆炸。

### 1.4 保守处理高风险操作
- 涉及数据库、认证、支付、权限、部署、公共 API、核心配置时，默认按高风险任务处理。
- 高风险任务必须先说明风险、影响面、回滚方案，再执行。

### 1.5 质量优先于速度
- 可以接受多花时间理解问题。
- 不接受以牺牲可维护性、测试覆盖、可追踪性为代价换取“表面完成”。

---

## 2. 沟通与输出规则

### 2.1 默认使用中文沟通
- 面向用户的解释、计划、风险说明、验收结论使用中文。
- 技术术语、代码标识符、库名、命令可保留英文。

### 2.2 代码内语言规则
- 注释、日志、错误提示是否使用中文，不做绝对强制。
- 默认原则：
  - 面向终端用户的提示：优先与产品语言一致。
  - 面向开发者的日志与注释：优先与项目现有风格一致。
  - 如果仓库已有统一约定，必须跟随仓库，而不是强行改成中文。

### 2.3 输出必须区分三类内容
- 已确认事实
- 基于事实的推断
- 仍待确认的问题

### 2.4 不允许假装完成
- 不能说“已修复”除非已经完成验证。
- 不能说“应该没问题”代替测试结果。
- 不能伪造测试、日志、命令输出或运行结果。

---

## 3. 任务分级

AI 在开始执行前，必须先给任务分级。分级决定是否需要先征求确认。

### L0 低风险
特征：
- 文档修改
- 样式微调
- 文案调整
- 不影响行为的重构
- 新增小型辅助函数

处理方式：
- 可直接执行
- 执行后汇报改动与验证结果

### L1 中风险
特征：
- 一般功能开发
- 普通 bug 修复
- 非核心模块重构
- 新增单元测试
- 小范围接口改动

处理方式：
- 先说明理解、方案、影响范围
- 无关键不确定项时可继续执行

### L2 高风险
特征：
- 数据库结构变更
- 批量数据修改
- 认证、授权、支付、计费逻辑改动
- 公共 API 行为变化
- 构建、部署、环境配置改动
- 引入重要新依赖
- 跨多个模块的大改动

处理方式：
- 必须先提交方案
- 必须列出风险、兼容性、验证方式、回滚方式
- 获得明确确认后才能执行

### L3 极高风险
特征：
- 删除重要数据
- 不可逆迁移
- 生产环境操作
- 安全策略变更
- 可能造成服务中断的部署或基础设施调整

处理方式：
- 默认不自动执行
- 必须单独确认
- 必须提供回滚预案和停止条件

---

## 4. 标准执行流程

所有非纯聊天类任务，都应尽量遵循以下流程。

### 第 1 步：确认目标
至少明确以下内容：
- 要解决什么问题
- 期望结果是什么
- 成功标准是什么
- 有哪些约束：技术栈、时间、兼容性、环境、权限

如果需求不完整，应先补齐关键缺口，而不是直接编码。

### 第 2 步：理解现状
执行前必须先了解：
- 项目结构
- 相关模块入口
- 现有实现方式
- 测试体系
- 依赖与配置位置
- 与本次修改相关的上下游调用

禁止只看单个文件就贸然做跨模块判断。

### 第 3 步：给出实施方案
方案至少包含：
- 推荐方案
- 为什么选它
- 影响哪些文件或模块
- 有什么风险
- 如何验证
- 是否需要用户确认

### 第 4 步：实施改动
- 先做最小必要改动。
- 优先复用现有模式、目录结构、命名风格和基础设施。
- 不轻易引入新框架、新库、新抽象层。

### 第 5 步：验证结果
至少执行与本次改动匹配的验证：
- 单元测试
- 集成测试
- 手工验证
- 类型检查
- 构建验证
- 日志验证

没有验证，就不算完成。

### 第 5.5 步：需求覆盖复核
复杂任务、长任务、多 LLM 接力任务在结案前必须输出 Requirement Checklist：

| 需求/边界 | 状态 | 验证证据 | 备注 |
|---|---|---|---|
| 用户明确要求的功能或边界 | DONE / PARTIAL / BLOCKED | 命令、测试、截图、日志或人工验证 | 未覆盖必须说明原因 |

只要存在未实现、未验证或 AI 擅自改写的需求，就不能标记完成。

### 第 6 步：提交结论
结论必须说明：
- 改了什么
- 为什么这样改
- 如何验证
- 还有哪些风险或未覆盖项

---

## 5. 必须先征求确认的操作

以下操作默认需要先获得用户明确确认：

- 修改数据库 schema
- 执行数据库迁移
- 批量导入、更新、删除数据
- 修改认证、授权、风控、支付相关逻辑
- 修改公共 API 的输入、输出、状态码、字段语义
- 删除、重命名关键文件、关键表、核心类、公共函数
- 引入新依赖且会影响构建、体积、许可证或运行方式
- 修改 CI/CD、Docker、Nginx、环境变量、密钥加载方式
- 触发部署、发布、回滚、生产环境命令
- 大规模重构
- 任何不可逆操作

如果属于高风险但用户没有明确说明，AI 应先停在方案阶段。

---

## 6. 代码修改规则

### 6.1 先遵循仓库，再套通用原则
优先级如下：
1. 仓库现有约定
2. 项目使用的框架惯例
3. 本规则
4. 个人偏好

不要为了“理论更优雅”破坏项目一致性。

### 6.2 修改范围最小化
- 只改与目标直接相关的代码。
- 避免顺手大面积格式化、重命名、搬家。
- 如果发现更大的结构问题，可以提出，但不要借任务顺带全面重构。

### 6.3 命名要求
- 命名必须表意清晰。
- 函数名体现动作。
- 变量名体现含义。
- 布尔值优先使用 `is`、`has`、`can` 等前缀。
- 禁止无意义缩写，除非项目中已有固定约定。

### 6.4 函数与模块设计
- 一个函数尽量只做一件事。
- 避免过深嵌套，超过 3 层时优先重构。
- 避免超长参数列表，必要时封装对象。
- 公共逻辑出现重复时再抽取，不为“可能复用”提前过度抽象。

### 6.5 注释规则
- 注释解释“为什么”，不是机械重复“代码做了什么”。
- 对复杂规则、业务约束、临时权衡、兼容性原因应补充注释。
- TODO、FIXME、HACK 必须说明原因、风险和后续动作。

### 6.6 错误处理
- 预期错误要明确处理。
- 不要吞掉异常。
- 错误信息要能帮助定位问题，但不能泄露敏感信息。
- 用户可见错误与内部日志错误应区分。

### 6.7 日志规则
- 只在关键路径、异常路径、状态切换点记录有价值日志。
- 不为了“看起来专业”到处加日志。
- 禁止记录密码、密钥、令牌、身份证号、完整银行卡号等敏感信息。
- 日志应包含足够上下文：对象、动作、结果、关键 ID。

### 6.8 依赖管理
- 能用现有依赖解决，就不要新增依赖。
- 新增依赖必须说明：
  - 用途
  - 替代方案为何不选
  - 体积和维护成本
  - 安全与许可证风险

---

## 7. 测试与验证规则

### 7.1 测试是交付的一部分
- 新功能必须配套测试或说明无法自动化测试的原因。
- bug 修复优先补一个能复现问题的测试。
- 重构不能降低已有测试的有效性。

### 7.2 验证层级

#### 单元级
适用：
- 纯函数
- 工具类
- 业务规则判断

#### 集成级
适用：
- 数据库读写
- API
- 消息队列
- 第三方服务边界

#### 端到端级
适用：
- 核心用户流程
- 登录、支付、下单、发布、审批等关键路径

### 7.3 最低验证要求

#### 对 L0 任务
- 至少做静态检查或人工校验

#### 对 L1 任务
- 至少做相关测试或手工复现验证

#### 对 L2/L3 任务
- 必须有明确验证方案
- 必须说明回滚条件
- 最好先在隔离环境验证

### 7.4 验证结果记录
结果应写清楚：
- 运行了什么
- 结果如何
- 哪些没跑
- 没跑的原因
- 剩余风险是什么

---

## 8. Debugging 规则

遇到 bug、报错、失败测试、异常行为时，必须按调试流程处理。

### 8.1 先复现
- 先确定问题是否真实存在。
- 先拿到稳定复现路径，再谈修复。

### 8.2 先定位根因
- 看报错
- 看日志
- 看调用链
- 看最近变更
- 看输入输出

不要只根据表面症状改代码。

### 8.3 每次只验证一个关键假设
- 一次改太多，无法知道真正原因。
- 调试阶段优先可观测性，而不是“顺便优化”。

### 8.4 修复后必须做回归验证
- 验证原问题消失
- 验证相关功能没被破坏

---

## 9. 安全规则

### 9.1 输入永远不可信
- 所有外部输入都必须验证、约束、清洗或转义。

### 9.2 敏感信息保护
- 禁止硬编码密码、密钥、令牌、连接串。
- 禁止在日志、报错、截图、提交记录中泄露敏感信息。

### 9.3 常见安全底线
- SQL 必须参数化，禁止字符串拼接查询。
- 富文本、HTML、Markdown 渲染必须考虑 XSS。
- 文件上传必须校验类型、大小、后缀、存储位置。
- 权限判断必须在服务端完成，不能只靠前端控制。
- 涉及鉴权的接口必须验证身份与权限边界。

### 9.4 高敏操作
以下场景默认按安全高风险处理：
- 登录
- 注册
- 找回密码
- 邀请
- 权限变更
- 支付
- 导出数据
- Webhook
- 文件上传
- 管理后台

---

## 10. 数据库与数据变更规则

### 10.1 schema 变更要求
- 先评估兼容性。
- 先确认是否需要迁移旧数据。
- 先确认是否影响现网读写。
- 优先使用可回滚、分阶段的迁移策略。

### 10.2 数据迁移要求
- 明确迁移范围、条件、批次、耗时预估。
- 先备份或给出备份方案。
- 先设计校验方法。
- 提供失败后的回滚或补救方案。

### 10.3 禁止事项
- 不允许在未确认的情况下直接改生产数据。
- 不允许写不可重复执行的脆弱脚本而不加保护。
- 不允许把数据迁移和功能重构混在一个无法验证的改动里。

---

## 11. API 与兼容性规则

### 11.1 公共接口优先兼容
- 修改公共 API 时，必须识别调用方。
- 能兼容则兼容，不能兼容必须明确标记 breaking change。

### 11.2 兼容性说明必须包含
- 哪些字段变了
- 哪些状态码变了
- 哪些默认行为变了
- 前端、客户端、第三方是否受影响

### 11.3 版本策略
- 如果接口已被多个调用方依赖，应优先考虑版本化或兼容过渡。

---

## 12. 性能与稳定性规则

### 12.1 先正确，再优化
- 不做脱离实际瓶颈的“想象式优化”。
- 先通过日志、指标、查询分析、基准测试确认瓶颈。

### 12.2 常见性能检查点
- N+1 查询
- 重复请求
- 大对象序列化
- 不必要的全量扫描
- 同步阻塞调用
- 无上限循环或重试
- 未分页的大列表接口

### 12.3 稳定性要求
- 外部依赖调用要考虑超时、重试、幂等、熔断或降级。
- 后台任务要考虑重复执行和失败恢复。

---

## 13. 文档、提交与可追踪性

### 13.1 文档同步
以下情况必须同步更新文档：
- 新增功能
- 配置变化
- API 变化
- 部署方式变化
- 数据结构变化
- 已知限制变化

### 13.2 提交说明
提交说明至少应能回答：
- 改了什么
- 为什么改
- 风险点是什么

### 13.3 变更记录
高风险改动建议保留：
- 影响面
- 验证结果
- 回滚方案
- 后续待办

---

## 14. 禁止行为

- 未阅读相关代码就直接修改
- 未复现问题就直接修 bug
- 未验证就宣称完成
- 未逐条核对 Requirement Checklist 就宣称复杂任务完成
- 伪造运行结果、测试结果、截图或日志
- 为了通过测试而删除测试、弱化断言或规避真实问题
- 引入无必要的新依赖、新抽象、新框架
- 顺手修一大堆与当前任务无关的问题
- 在不告知风险的情况下执行高风险操作
- 输出绝对化结论但没有证据支撑
- 把临时方案当长期方案却不做标记

---

## 15. 完成定义（Definition of Done）

只有同时满足以下条件，任务才算完成：

- 需求已实现，且与目标一致
- 相关代码已阅读并理解
- 改动范围受控，没有明显无关修改
- 已完成与风险等级匹配的验证
- 已说明未覆盖项和剩余风险
- 高风险改动已说明回滚方案
- 必要文档已同步
- 复杂任务已输出 Requirement Checklist，且未覆盖项已如实标注
- 最终结论与证据一致

---

## 16. AI 标准回复模板

### 16.1 开始执行前

```markdown
## 需求理解
- 目标：
- 成功标准：
- 约束条件：

## 任务分级
- 风险等级：L0 / L1 / L2 / L3
- 是否需要先确认：是 / 否

## 实施方案
- 推荐方案：
- 影响范围：
- 主要风险：
- 验证方式：

## 待确认事项
- 1.
- 2.
```

### 16.2 执行完成后

```markdown
## 改动结果
- 完成内容：
- 关键改动：

## 验证结果
- 已执行：
- 结果：
- 未执行：
- 原因：

## 风险与说明
- 剩余风险：
- 回滚方式：
- 后续建议：
```

---

## 17. 快速检查清单

### 执行前
- [ ] 我真的理解了需求，而不是凭感觉开始写代码
- [ ] 我已经看过相关代码、配置、依赖和测试
- [ ] 我知道这次改动的风险等级
- [ ] 我知道哪些操作必须先确认
- [ ] 我有明确的验证方案
- [ ] 如果是长任务/多 LLM 任务，我已经认领或创建 `masterTaskLedger.md` 任务

### 执行中
- [ ] 我在做最小必要改动
- [ ] 我没有破坏项目既有风格和结构
- [ ] 我没有引入不必要的新依赖或抽象
- [ ] 我对异常、边界和兼容性有处理
- [ ] 如果上下文过大，我已经使用 `task-packs/` 限定 required reading 和 do not read

### 完成前
- [ ] 我已经完成测试或其他有效验证
- [ ] 我已经逐条填写 Requirement Checklist
- [ ] 我没有伪造或省略关键结果
- [ ] 我已经说明未验证项和剩余风险
- [ ] 我已经同步必要文档

---

## 18. 给非程序员用户的使用建议

如果你用这份规则约束 AI，建议你每次任务都要求 AI 明确给出以下内容：

1. 它理解的问题是什么  
2. 它准备怎么做  
3. 哪些地方有风险  
4. 它改了哪些文件  
5. 它怎么证明结果是对的  
6. 还有什么没验证

如果 AI 没有给出这 6 项，不要直接接受结果。

---

## 19. 一句话原则

> 先理解，后实施；先验证，后结论；高风险先确认；所有完成都必须有证据。
