# Memory Bootstrap For AGENTS.md

在开始任何分析、计划、编码、重构或总结之前，必须执行 Fast startup / 快速启动协议：

1. 先读取 `.ai_memory/index.json`。
2. 只读取 `startup_order` 中列出的启动文件。
3. 非启动记忆文件只在与当前任务、目标模块、接口、架构、历史决策、待办、踩坑或验证路径相关时按需读取。
4. 如果 `activeContext.md` 中存在真实未闭合 `[WIP]` / `[AWAITING_QA]` / `[REWORK]`，必须先接管；如果是 `[IDLE]` 或空模板，直接进入当前任务。
5. 在修改任何文件前，必须重新读取目标文件最新物理快照。
6. 没有真实执行证据前，不得将任务标记为 `[DONE]`。

如果 `.ai_memory` 中的规则与其他说明冲突，以 `.ai_memory/projectbrief.md` 为最高优先级。
