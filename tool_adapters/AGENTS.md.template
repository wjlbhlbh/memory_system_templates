# Memory Bootstrap For AGENTS.md

开始工作前执行状态感知启动：

1. 只读取 `.ai_memory/activeContext.md`；启动时不得读取 `index.json`、`projectbrief.md` 或其他记忆文件。
2. 若状态为 `[IDLE]` / `[PARKED]`，或旧任务不匹配用户最新要求，直接执行最新要求，不加载旧任务包。
3. 只有用户确实在继续当前任务时，才读取胶囊指定的精确 `Task pack` 与 `Resume Reads`。
4. 需求、架构、模块、决策、踩坑和历史只在当前任务需要时按需读取；历史先检索索引。
5. 大文件先搜索定位再分段读取；不得把完整代码、长日志或命令输出写进记忆。
6. `activeContext.md` 只保存任务 ID、状态、目标、checkpoint、下一步和指针；详细内容进入任务包。
7. 实施前核对目标文件最新物理快照；完成前提供真实验证证据，并区分实现、验证、迁移、部署与真实验收。
8. 用户最新明确要求优先；高风险或不可逆操作仍按平台与项目审批边界执行。
9. Use the available file read, search, edit, and write capability in the current environment or an equivalent capability.

## Context compaction
- 压缩摘要只保留任务 ID、当前目标、最近验证 checkpoint、变更路径、验证结果、阻塞项和下一步。
- 禁止保留完整文件、完整工具输出、旧摘要正文、启动文件或规则正文。
- 压缩后只重读 `activeContext.md`；确需续接时再读它指向的任务包。
- 不相关的新任务使用干净会话，不续接旧对话。

## Requirement lifecycle
- 首份真实需求到达且基线未初始化时，按需读取并初始化 `requirements/current.md`。
- Latest User Intent Wins；旧版本记为 `SUPERSEDED`，需求同步不等于实现或验证完成。

## 经验复用（按需）
- 确定任务后，遇到相关工具操作或首次失败准备重试时，搜索项目 `.ai_memory/pitfalls.md`；若 `AI_EXPERIENCE_DIR` 已设置，同时搜索其中 `entries/EXP-*.md`。只读命中的经验，不在启动时预读。
- 解决办法经验证且有长期价值时，先查重：项目专属经验写项目记忆，跨项目通用经验在共享目录新增独立文件；不写密钥、隐私数据或完整日志。
